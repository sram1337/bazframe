# Sanitized MTG source-unit fixture

Provider-prepared Stage 2 experiment input. This tree contains only two Agent Skills, transformed pure shared code, two exact approved references, synthetic immutable inputs, and a locked `tsx` runtime. Direct child-root execution is source-tree development evidence, not an installed or managed command contract.
