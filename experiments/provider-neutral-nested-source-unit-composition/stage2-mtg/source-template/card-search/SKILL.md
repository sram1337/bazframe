---
name: card-search
description: Search a sanitized immutable MTG card fixture through shared group code.
---

# Card Search

This experiment child uses `scripts/card-search.ts`, group-level shared card modules, exact approved references, synthetic immutable inputs, and the group-root locked TypeScript runtime.

The direct child-root command is source-tree development evidence only. It is not an installed command, Bazframe gateway, or lifecycle guarantee.
