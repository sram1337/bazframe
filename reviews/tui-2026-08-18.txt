- shift + direction doesnt change pane focus
- profiles text should be left allign profile name right align num skills
- when on a smaller screen width and you click a profile, the height of the skills boxes are too small - only one visibile line.
- There are 3 places with instructions for hotkeys and they are all different - inconsistent.
- Warning should disappear after an action occurs (keypress)
- on settings and adapters, make the sections tabbed to give visual hierarchy.
- Adapters should be a list of enabled and options to enable below (like askills). Right now the list is just containing Pi. But you can enable/disable it and it will move between the lists (and enable disable itself in the harness extension area) Also there should be a status. By default Pi harness needs to be installed and will say that next to it in the list. Installing will enable it. Then you can disable/enable it. Or uninstall.
- settings is odd. They shouldn't be read only. I don't think any of that stuff is interest thats there except maybe the active profile. What is a global policy? Its enabled? What does that mean? Current director is different than whether something is git enabled.
- press 'e' on the skilllist pane when a skill is selected, or on that skills SKILL.md to open the skill at the location in your $EDITOR, warning if the editor is unset.
- profiles selected pane needs to be able to also show the AGENTS.md for that profile and if it will apply (ie pazframe is in the proper mode.)
- making the window ~80 columns makes the 'Selected skills' get overridden by the actual skills themselves, so theres a visual glitch.
- like on ths skills page, skills in the select/available skills should be grouped under their source.
- i dont think we need both source and namespace. 
- moveing the selection in the tab section when focused should just automatically change the window to that tab. Tabbing into it should just select the current tab (so nothing switches). 
