- profiles shows empty until it loads. show loading or something. Cache? Make it faster
- resizing terminal by increasing window size, does not result in vertical refresh, instead adding blank lines to the top of the screen. A resize reducing the vertical height will then immediately set the content to the right location (removing the blank lines at the top resulting in a vertical shift)
- leave a note on how to switch tabs (at the top)
- (o)pen and (c)ollapse on skills page to open and collapse the current source - if on a skill inside a source, will collapse the parent source
- skill page should have two panes; show the SKILL.md in the right pane. For smaller screens, pressing enter on the skill will swap the current frame with the render frame and backspace/esc will have you return.
- backspace should allow return
- there should be a "back" sort of arrow rended in the top left of the screen to indicate you can go back, also maybe somehow cisually denote you are in a nested "menu" and can go back. Maybe breadcrumb or something idk.
- settings can be broken into subsections
- the info in settings doesn't make sense to me and has no logical grouping. 
- Sources/skills should have a way to add new sources with a file browser where you can type the cwd and it updates the browser in real time. So i can type '~/Down' and it will automatically be showing my downloads folder (nested below parents) and I can select a skill folder. 
- skills and sources can probably go into one tab - 'Skills'.
- Skills, Profiles, Adapters, Settings
- Profiles should be dual pane, with the current skills in the profile showing up on the right, with similar mechanics for smaller screens. 
